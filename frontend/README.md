
# Frontend (Tailwind + HTMX + Alpine)

- يبني Tailwind إلى: `../backend/static/css/app.css`
- ينسخ سكربت المشروع إلى: `../backend/static/js/app.js`

## أوامر NPM
```bash
cd frontend
npm install
npm run dev   # مراقبة مستمرة
npm run build # إنتاج (Minified)
```
